<div class="row justify">
    <div class="col-12 text-center mt-2">
        <h1>Aggiungi Recensione per <strong><%=@bar.name%></strong></h1>
    </div>
</div>
<hr/>

<%= render 'form', review: @review %>
